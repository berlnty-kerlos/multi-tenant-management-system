# Postgres config
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=management_system_test_db
POSTGRES_PORT=5432

# Backend database URL (Docker-friendly)
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:${POSTGRES_PORT}/${POSTGRES_DB}

# JWT configuration
JWT_SECRET_KEY=supersecretkey
JWT_REFRESH_SECRET_KEY=superrefreshsecretkey
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Ports
BACKEND_PORT=8000
FRONTEND_PORT=3000
